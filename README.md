# Bitcoin-Address-HASH160-Converter
his Python script decodes Bitcoin addresses (legacy, P2SH, and Bech32 types) and extracts their underlying HASH160 or witness program in hexadecimal format. It supports mainnet and testnet, validates checksums, and safely handles invalid or malformed addresses.
